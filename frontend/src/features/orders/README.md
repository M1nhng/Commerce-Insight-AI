# Feature: Orders

Orders feature: Order list, order detail, order status management.

## Structure

`
orders/
├── components/    # Feature-specific UI components
├── hooks/         # Custom hooks (data fetching, state)
├── services/      # API calls (uses Axios + TanStack Query)
├── types/         # TypeScript type definitions
└── index.ts       # Public barrel export
`

## Status

🚧 Not yet implemented. See [ROADMAP](../../../docs/15_ROADMAP.md).
