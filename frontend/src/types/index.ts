/**
 * Shared TypeScript type definitions for Commerce Insight AI frontend.
 *
 * TODO: Populate when API contracts are defined in docs/07_API.md.
 *
 * Planned types:
 *   - ApiResponse<T>       — Standard API response wrapper
 *   - PageResponse<T>      — Paginated response
 *   - ApiError             — Error response structure
 *   - User                 — Authenticated user
 *   - Role                 — User role enum
 *   - Product              — Product model
 *   - Order                — Order model
 *   - OrderStatus          — Order status enum
 *   - AnalyticsKPI         — KPI metric
 *   - AiInsight            — AI-generated insight
 */

export type {}
