/**
 * Shared TypeScript types for the MCP Server.
 *
 * These interfaces mirror the backend DTO contracts defined in docs/07_API.md.
 * Update these types in sync with any backend DTO changes.
 *
 * TODO: Populate when API contracts are finalized.
 *
 * Planned types:
 *   - ApiResponse<T>       — Standard API response wrapper
 *   - PageResponse<T>      — Paginated response
 *   - ApiError             — Error response structure
 *   - ProductSummary       — Product list item DTO
 *   - OrderSummary         — Order list item DTO
 *   - RevenueSummary       — Analytics revenue DTO
 *   - AiInsightResponse    — AI insight DTO
 */

export type {}
