-- =============================================================================
-- V3 — Indexes & Constraints
-- Commerce Insight AI
-- =============================================================================
-- TODO: Implement indexes when V1 schema is finalized.
--       Performance-critical indexes and additional constraints go here.
-- =============================================================================
--
-- Planned indexes:
--   - users(email) UNIQUE
--   - products(sku) UNIQUE
--   - orders(status, created_at) — for analytics queries
--   - order_items(order_id)
--   - ai_insights(user_id, created_at)
-- =============================================================================

-- Placeholder: indexes will be populated in the implementation phase.
SELECT 1;
