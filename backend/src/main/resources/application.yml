spring:
  application:
    name: commerce-insight-ai

  # ── Multipart upload ──────────────────────────────────────────────────────
  # Spring Boot's own defaults (max-file-size=1MB, max-request-size=10MB) sit
  # below app.import.max-file-size-mb (see below) and rejected uploads with a
  # 413 before that check ever ran. Bound to the SAME env var so raising one
  # knob raises the real ceiling everywhere; import only ever sends one file
  # per request, so max-request-size tracks max-file-size.
  servlet:
    multipart:
      max-file-size: ${IMPORT_MAX_FILE_SIZE_MB:10}MB
      max-request-size: ${IMPORT_MAX_FILE_SIZE_MB:10}MB

  # ── DataSource ──────────────────────────────────────────────────────────
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:commerce_insight}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
    hikari:
      pool-name: CommerceInsightPool
      maximum-pool-size: 10
      minimum-idle: 2
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-test-query: SELECT 1

  # ── JPA / Hibernate ──────────────────────────────────────────────────────
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
        jdbc:
          time_zone: UTC
        globally_quoted_identifiers: false
        default_batch_fetch_size: 20
    open-in-view: false

  # ── Flyway ───────────────────────────────────────────────────────────────
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false
    validate-on-migrate: true
    out-of-order: false
    fail-on-missing-locations: true

  # ── Jackson ──────────────────────────────────────────────────────────────
  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false
      indent-output: false
    deserialization:
      fail-on-unknown-properties: false
    time-zone: UTC

  # ── Web ──────────────────────────────────────────────────────────────────
  web:
    resources:
      add-mappings: false

# ── Server ────────────────────────────────────────────────────────────────
server:
  port: ${SERVER_PORT:8080}
  servlet:
    context-path: /
  compression:
    enabled: true
    mime-types: application/json,application/xml,text/html,text/xml,text/plain
    min-response-size: 1024
  error:
    include-message: never
    include-stacktrace: never
    include-exception: false

# ── Actuator ──────────────────────────────────────────────────────────────
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
      base-path: /actuator
  endpoint:
    health:
      show-details: when-authorized

# ── Logging ───────────────────────────────────────────────────────────────
logging:
  level:
    root: INFO
    com.commerceinsight: INFO
    org.springframework.security: WARN
    org.hibernate.SQL: WARN
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] [%X{requestId:-}] %-5level %logger{36} - %msg%n"

# ── Application Custom Properties ─────────────────────────────────────────
app:
  # JWT
  jwt:
    secret: ${JWT_SECRET:CommerceInsightAIDevSecretKeyMustBe256BitsLongForHMACSHA256OK}
    access-token-expiration-ms: ${JWT_ACCESS_EXPIRATION_MS:900000}       # 15 minutes
    refresh-token-expiration-days: ${JWT_REFRESH_EXPIRATION_DAYS:7}       # 7 days
    issuer: commerce-insight-ai

  # CORS
  cors:
    allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174,http://localhost:3000}
    allowed-methods: GET,POST,PUT,PATCH,DELETE,OPTIONS
    allowed-headers: ${CORS_ALLOWED_HEADERS:Authorization,Content-Type,Accept,Origin,X-Requested-With,X-Request-Id}
    # Tokens are carried in the response body, not cookies — credentials are not required.
    allow-credentials: ${CORS_ALLOW_CREDENTIALS:false}
    max-age: 3600

  # MCP Server
  mcp:
    api-key: ${MCP_API_KEY:mcp-dev-secret-key-change-in-production}

  # Security hardening (Sprint 12A)
  security:
    # Comma-separated IPs / CIDR ranges whose X-Forwarded-For / X-Real-IP is trusted.
    # Empty = trust nothing (use the direct peer address).
    trusted-proxies: ${TRUSTED_PROXIES:}
    request-id-header: X-Request-Id

  # Rate limiting (Sprint 12A) — in-memory (bucket4j), per rolling window.
  rate-limit:
    enabled: ${RATE_LIMIT_ENABLED:true}
    login:
      capacity: 5
      window-seconds: 60
      secondary-capacity: 20
      secondary-window-seconds: 3600
    register:
      capacity: 5
      window-seconds: 3600
    refresh:
      capacity: 30
      window-seconds: 60
    import-upload:
      capacity: 10
      window-seconds: 60
    export:
      capacity: 10
      window-seconds: 60
    # AI insights generation — one expensive LLM call per request.
    ai-insights:
      capacity: 10
      window-seconds: 3600

  # AI-assisted analytics insights (optional feature). Disabled unless a
  # provider is configured; the dashboard works with it off. No secret has a
  # committed default — api-key is empty until you supply OPENAI_API_KEY.
  ai:
    enabled: ${AI_INSIGHTS_ENABLED:false}
    provider: ${AI_PROVIDER:openai}          # openai | ollama | anthropic | any OpenAI-compatible gateway
    model: ${AI_MODEL:gpt-4o-mini}
    # api-key: OpenAI/gateway key for the openai/ollama family, or the Anthropic
    # key when provider=anthropic. Empty for a keyless local Ollama.
    api-key: ${AI_API_KEY:${OPENAI_API_KEY:${ANTHROPIC_API_KEY:}}}
    base-url: ${AI_BASE_URL:https://api.openai.com/v1}          # OpenAI-compatible chat URL
    anthropic-base-url: ${ANTHROPIC_BASE_URL:https://api.anthropic.com}
    anthropic-version: ${ANTHROPIC_VERSION:2023-06-01}
    timeout-ms: ${AI_TIMEOUT_MS:20000}
    connect-timeout-ms: ${AI_CONNECT_TIMEOUT_MS:5000}
    max-output-tokens: ${AI_MAX_OUTPUT_TOKENS:900}
    temperature: ${AI_TEMPERATURE:0.2}
    max-range-days: 365
    max-context-chars: 12000

  # Data Import
  import:
    max-file-size-mb: ${IMPORT_MAX_FILE_SIZE_MB:10}
    max-rows: ${IMPORT_MAX_ROWS:5000}

  # Report Export (Sprint 11A)
  export:
    max-rows: ${EXPORT_MAX_ROWS:10000}

  # OpenAPI
  openapi:
    title: Commerce Insight AI API
    description: AI-powered Ecommerce Analytics Platform REST API
    version: 1.0.0
    contact-name: Commerce Insight AI Team
    contact-email: api@commerceinsight.ai
